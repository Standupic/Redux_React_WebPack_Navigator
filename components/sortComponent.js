import React from 'react';

const SortComponent = (props) => {
    var obj = {
        default: "Cортировать",
        
    }
    return(
        <select className="an-styler" ref={(el) => anStyler = el}>

        </select>
    )
}